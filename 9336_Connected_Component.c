#include <stdio.h>
#include <string.h>

#define MAX 200000
int arr[MAX];
int main(int argc, char *argv[])
{
    int n, k;
    while (~scanf("%d%d", &n, &k) && n) {
        memset(arr, 0, n*sizeof(arr));

    }
    return 0;
}
