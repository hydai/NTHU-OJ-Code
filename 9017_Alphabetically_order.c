#include <stdio.h>
#include <string.h>

#define MAX 100

int main(int argc, char *argv[])
{
    char str1[MAX], str2[MAX];
    int len1, len2, is_first = 1;
    while (~scanf("%s%s", str1, str2)) {
        len1 = strlen(str1); len2 = strlen(str2);
        while (len1 && len2) {
            
        }
    }
    return 0;
}
